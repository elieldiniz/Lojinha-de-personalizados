/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
      domains: ['firebasestorage.googleapis.com','i.postimg.cc'], // Adicione o domínio aqui
  },
}

module.exports = nextConfig;